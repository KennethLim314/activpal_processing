from setuptools import setup, find_packages

setup(
    name='activepal_tools',
    version='0.0.1',
    url='',
    author='Kenneth Lim',
    author_email='kenneth@goodbiome.com',
    description='Library for the processing of activepal data',
    packages=find_packages(),
    install_requires=[],
)
